services:
  app:
    build: .
    container_name: nextjs-app
    restart: unless-stopped
    env_file:
      - .env
    expose:
      - "4010"
    networks:
      - app-network

 networks:
  app-network:
    external: true
    name: amp_portal_backend_app-network
